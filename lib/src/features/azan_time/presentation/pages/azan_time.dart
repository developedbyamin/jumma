import 'package:flutter/material.dart';

class AzanTime extends StatelessWidget {
  const AzanTime({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Azan time'),
    );
  }
}
