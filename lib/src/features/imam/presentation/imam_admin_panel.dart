import 'package:flutter/material.dart';

class ImamAdminPanel extends StatelessWidget {

  const ImamAdminPanel({ super.key });

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text('awdawd'),),
           body: Container(
            child: Center(
              child: Text('imammmmm'),
            ),
           ),
       );
  }
}